import React from 'react'
import { BrowserRouter as Router, Route } from "react-router-dom";
/*
const PrivateRoute: React.FC<{ path: string, component: React.FC, isLoggedIn: boolean }> = ({
    path,
    component: Component,
    isLoggedIn
  }) => {
    return (
      <Route path={path} render={() => (
                                 isLoggedIn ? (
            <Component />
          ) : (
            <Redirect to="/" />
          )
        )}
      />
    );
  };
*/
//export default PrivateRoute